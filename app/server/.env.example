# L'API key NON va impostata manualmente se avvii il server dal terminale di Claude Code —
# Claude Code la inietta automaticamente nell'ambiente del processo figlio.
#
# Serve solo se avvii il server al di fuori di Claude Code:
# ANTHROPIC_API_KEY=sk-ant-...

PORT=3000
